import { json,error,readJson } from '../../lib/response.js';
import { all,first,run,makeId,nowIso } from '../../lib/db.js';
import { requireTenant,canViewFinance } from '../../lib/permissions.js';
const text=(value,max=5000)=>value===null||value===undefined?null:(String(value).trim().slice(0,max)||null);
function requireFinance(auth){if(!canViewFinance(auth)){const e=new Error('Finance permission is required');e.status=403;throw e;}}
export async function onRequestGet(context){try{const auth=context.data.auth;requireFinance(auth);const tenantId=requireTenant(auth);if(!context.env.DB)return json({items:[],total:0,demo:true});const items=await all(context.env.DB,`SELECT pay.*,p.name AS project_name,c.name AS campaign_name FROM payments pay JOIN projects p ON p.id=pay.project_id AND p.tenant_id=pay.tenant_id LEFT JOIN campaigns c ON c.id=pay.campaign_id AND c.tenant_id=pay.tenant_id WHERE pay.tenant_id=? ORDER BY COALESCE(pay.received_date,pay.due_date,'9999-12-31') DESC`,[tenantId]);return json({items,total:items.length});}catch(cause){return error(cause.message||'Payments could not be loaded',Number(cause.status||500));}}
export async function onRequestPost(context){try{const auth=context.data.auth;requireFinance(auth);const tenantId=requireTenant(auth);const body=await readJson(context.request);const projectId=text(body.projectId,120);const amount=Number(body.amount);if(!projectId||!Number.isFinite(amount)||amount<0)return error('Project and valid amount are required',422);if(!context.env.DB)return json({id:makeId('pay'),created:true,demo:true},201);const project=await first(context.env.DB,'SELECT id FROM projects WHERE tenant_id=? AND id=?',[tenantId,projectId]);if(!project)return error('Project not found',404);const id=makeId('pay');const now=nowIso();const status=String(body.status||'INVOICED').toUpperCase();await run(context.env.DB,`INSERT INTO payments (id,tenant_id,project_id,campaign_id,invoice_reference,payment_type,amount,currency,amount_base_currency,due_date,received_date,status,payment_method,wallet_or_bank_reference,notes,created_at,updated_at) VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)`,[id,tenantId,projectId,text(body.campaignId,120),text(body.invoiceReference,300),text(body.paymentType,200),amount,text(body.currency,10)||'USD',amount,text(body.dueDate,30),text(body.receivedDate,30),status,text(body.paymentMethod,200),text(body.reference,1000),text(body.notes,10000),now,now]);await run(context.env.DB,`INSERT INTO audit_logs (id,tenant_id,user_id,action,entity_type,entity_id,after_data,created_at) VALUES (?,?,?,'PAYMENT_CREATED','PAYMENT',?,?,?)`,[makeId('aud'),tenantId,auth.userId,id,JSON.stringify({projectId,amount,status}),now]);return json({id,created:true},201);}catch(cause){return error(cause.message||'Payment could not be created',Number(cause.status||500));}}
