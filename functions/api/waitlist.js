import { json,error,readJson } from '../lib/response.js';
const text=(v,max=500)=>String(v??'').trim().slice(0,max);
const allowedBudgets=new Set(['Under $50','$50–$100','$100–$250','$250–$500','$500–$1,000','$1,000+','Enterprise / Let\'s talk']);
export async function onRequestPost(context){try{const body=await readJson(context.request);if(text(body.website,200))return json({joined:true});const name=text(body.name,120),email=text(body.email,240).toLowerCase(),company=text(body.company,180),consent=body.consent==='yes';if(!name||!company||!/^\S+@\S+\.\S+$/.test(email)||!consent)return error('Please complete the required fields and consent to contact',422);const monthlyBudget=text(body.monthlyBudget,80);if(!allowedBudgets.has(monthlyBudget))return error('Please select a valid monthly budget range',422);const features=Array.isArray(body.priorityFeatures)?body.priorityFeatures.map(v=>text(v,80)).filter(Boolean).slice(0,3):[];if(!context.env.DB)return json({joined:true,demo:true});await context.env.DB.prepare(`CREATE TABLE IF NOT EXISTS public_waitlist (id TEXT PRIMARY KEY,email TEXT NOT NULL UNIQUE,name TEXT NOT NULL,company TEXT NOT NULL,company_type TEXT,team_size TEXT,current_system TEXT,primary_use_case TEXT,monthly_budget TEXT,priority_features_json TEXT,consent_at TEXT NOT NULL,source TEXT NOT NULL,created_at TEXT NOT NULL)`).run();const now=new Date().toISOString();const id=`wait_${crypto.randomUUID()}`;await context.env.DB.prepare(`INSERT INTO public_waitlist (id,email,name,company,company_type,team_size,current_system,primary_use_case,monthly_budget,priority_features_json,consent_at,source,created_at) VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?) ON CONFLICT(email) DO UPDATE SET name=excluded.name,company=excluded.company,company_type=excluded.company_type,team_size=excluded.team_size,current_system=excluded.current_system,primary_use_case=excluded.primary_use_case,monthly_budget=excluded.monthly_budget,priority_features_json=excluded.priority_features_json,consent_at=excluded.consent_at`).bind(id,email,name,company,text(body.companyType,100),text(body.teamSize,50),text(body.currentSystem,100),text(body.primaryUseCase,120),monthlyBudget,JSON.stringify(features),now,'PUBLIC_HOME',now).run();const count=await context.env.DB.prepare('SELECT COUNT(*) AS total FROM public_waitlist').first();return json({joined:true,position:Number(count?.total||0)});}catch(cause){return error(cause.message||'Unable to join the waitlist',Number(cause.status||500));}}
export async function onRequestGet(){return error('Method not allowed',405);}
