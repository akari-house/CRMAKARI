(() => {
  'use strict';
  if(window.__akariCampaignGtmTrackingR44)return;
  window.__akariCampaignGtmTrackingR44=true;
  const cache=new Map();
  const types=['X_SPACE','AMA','PR','PARTNERSHIP','LAUNCHPAD','EXCHANGE','COMMUNITY','EVENT','LISTING','OTHER'];
  const statuses=['PLANNED','LIVE','COMPLETED','CANCELLED'];
  const esc=(v)=>String(v??'').replace(/[&<>'"]/g,(c)=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
  const fmt=(v,d=0)=>Number(v||0).toLocaleString(undefined,{maximumFractionDigits:d});
  const label=(v)=>String(v||'').replaceAll('_',' ').replace(/\b\w/g,(c)=>c.toUpperCase());
  async function api(id,options={}){const r=await fetch(`/api/campaign-gtm-tracking/${encodeURIComponent(id)}`,{...options,headers:{'content-type':'application/json',...(options.headers||{})}});const p=await r.json().catch(()=>({}));if(!r.ok)throw new Error(p.error||p.message||'GTM tracking request failed');return p;}
  function closeModal(){document.querySelector('.gtm-modal-r44')?.remove();}
  function activityModal(id,current={}){
    closeModal(); const layer=document.createElement('div'); layer.className='gtm-modal-r44'; const today=new Date().toISOString().slice(0,10);
    layer.innerHTML=`<form><h3>${current.id?'Edit tracked activity':'Track GTM activity'}</h3><div class="gtm-form-r44"><input type="hidden" name="id" value="${esc(current.id||'')}"><label>Type<select name="type">${types.map((t)=>`<option value="${t}" ${t===(current.type||'X_SPACE')?'selected':''}>${label(t)}</option>`).join('')}</select></label><label>Status<select name="status">${statuses.map((s)=>`<option value="${s}" ${s===(current.status||'PLANNED')?'selected':''}>${label(s)}</option>`).join('')}</select></label><label class="full">Title<input name="title" value="${esc(current.title||'')}" required></label><label>Date<input type="date" name="dataDate" value="${esc(current.dataDate||today)}" required></label><label>Partner / publication<input name="partner" value="${esc(current.partner||'')}"></label><label>Platform<input name="platform" value="${esc(current.platform||'')}"></label><label>Owner<input name="ownerName" value="${esc(current.ownerName||'')}"></label><label class="full">URL<input name="url" value="${esc(current.url||'')}"></label><label>Reach<input type="number" min="0" name="reach" value="${esc(current.reach||'')}"></label><label>Impressions<input type="number" min="0" name="impressions" value="${esc(current.impressions||'')}"></label><label>Engagements<input type="number" min="0" name="engagements" value="${esc(current.engagements||'')}"></label><label>Attendees<input type="number" min="0" name="attendees" value="${esc(current.attendees||'')}"></label><label>Clicks<input type="number" min="0" name="clicks" value="${esc(current.clicks||'')}"></label><label>Leads<input type="number" min="0" name="leads" value="${esc(current.leads||'')}"></label><label>Applications<input type="number" min="0" name="applications" value="${esc(current.applications||'')}"></label><label>Meetings<input type="number" min="0" name="meetings" value="${esc(current.meetings||'')}"></label><label class="full">Outcome / notes<textarea name="notes" rows="3">${esc(current.notes||'')}</textarea></label></div><div class="gtm-modal-actions-r44"><button type="button" data-close>Cancel</button><button type="submit" class="primary">Save activity</button></div></form>`;
    layer.addEventListener('click',(e)=>{if(e.target===layer||e.target.closest('[data-close]'))closeModal();});
    layer.querySelector('form').addEventListener('submit',async(e)=>{e.preventDefault();const b=e.submitter;b.disabled=true;try{await api(id,{method:'PATCH',body:JSON.stringify({action:'upsert-activity',activity:Object.fromEntries(new FormData(e.currentTarget).entries())})});closeModal();await load(id,true);}catch(c){alert(c.message);b.disabled=false;}}); document.body.appendChild(layer);
  }
  function render(id,payload){
    const workspace=document.querySelector('.delivery-workspace'); const body=workspace?.querySelector('.delivery-workspace-body'); if(!workspace||!body)return; workspace.querySelector('.campaign-gtm-tracking-r44')?.remove();
    const item=payload.item,s=item.summary||{},activities=item.activities||[];
    const panel=document.createElement('section'); panel.className='campaign-gtm-tracking-r44';
    panel.innerHTML=`<header><div><span>GTM ACTIVITY INTELLIGENCE</span><strong>PR, Spaces, partnerships & launch tracking</strong><small>Record what happened and measure outcomes. The CRM does not execute the activity.</small></div>${payload.permissions?.canWrite?'<button class="primary" data-add>Track activity</button>':''}</header><div class="gtm-kpis-r44"><article><small>Activities</small><b>${fmt(s.activityCount)}</b><span>${fmt(s.completedCount)} completed</span></article><article><small>Upcoming</small><b>${fmt(s.upcomingCount)}</b><span>Planned activities</span></article><article><small>Reach</small><b>${fmt(s.totalReach)}</b><span>${fmt(s.totalImpressions)} impressions</span></article><article><small>Engagements</small><b>${fmt(s.totalEngagements)}</b><span>${fmt(s.totalAttendees)} attendees</span></article><article><small>Outcomes</small><b>${fmt(s.totalLeads)} leads</b><span>${fmt(s.totalMeetings)} meetings · ${fmt(s.totalApplications)} apps</span></article></div><div class="gtm-layout-r44"><section><div class="gtm-section-head-r44"><strong>Activity log</strong><span>Newest first</span></div><div class="gtm-activity-list-r44">${activities.length?activities.slice(0,12).map((a)=>`<article data-activity="${esc(a.id)}"><div class="gtm-type-r44">${label(a.type)}</div><div><strong>${esc(a.title)}</strong><span>${esc(a.dataDate)} · ${label(a.status)}${a.partner?` · ${esc(a.partner)}`:''}</span></div><div><b>${fmt(a.reach)} reach</b><span>${fmt(a.engagements)} eng. · ${fmt(a.leads)} leads</span></div>${a.url?`<a href="${esc(a.url)}" target="_blank" rel="noopener">Open</a>`:''}${payload.permissions?.canWrite?'<button data-edit>Edit</button>':''}</article>`).join(''):'<p class="gtm-empty-r44">No GTM activities tracked yet.</p>'}</div></section><section><div class="gtm-section-head-r44"><strong>Channel roll-up</strong><span>Automatically derived</span></div><div class="gtm-type-list-r44">${s.typeBreakdown?.length?s.typeBreakdown.map((t)=>`<article><div><strong>${label(t.type)}</strong><span>${fmt(t.completed)} / ${fmt(t.count)} completed</span></div><div><b>${fmt(t.reach)} reach</b><span>${fmt(t.engagements)} eng. · ${fmt(t.leads)} leads</span></div></article>`).join(''):'<p class="gtm-empty-r44">Channel totals will appear automatically.</p>'}</div></section></div>`;
    panel.querySelector('[data-add]')?.addEventListener('click',()=>activityModal(id)); panel.querySelectorAll('[data-activity]').forEach((row)=>{const activity=activities.find((a)=>a.id===row.dataset.activity);row.querySelector('[data-edit]')?.addEventListener('click',()=>activityModal(id,activity));});
    const creator=body.querySelector('.campaign-creator-tracking-r43'); if(creator)creator.insertAdjacentElement('afterend',panel); else body.appendChild(panel); workspace.dataset.gtmTrackingR44=id;
  }
  async function load(id,force=false){if(!id)return;if(!force&&cache.has(id))return render(id,cache.get(id));try{const p=await api(id);cache.set(id,p);render(id,p);}catch(c){console.warn('[AKARI GTM tracking]',c);}}
  const originalFetch=window.fetch.bind(window); window.fetch=async(...args)=>{const response=await originalFetch(...args);try{const url=typeof args[0]==='string'?args[0]:args[0]?.url||'';const match=url.match(/\/api\/service-delivery\/([^/?#]+)$/);if(match&&response.ok)queueMicrotask(()=>load(decodeURIComponent(match[1]),true));}catch{}return response;};
  new MutationObserver(()=>{const workspace=document.querySelector('.delivery-workspace');if(!workspace||workspace.querySelector('.campaign-gtm-tracking-r44'))return;const id=[...cache.keys()].at(-1);if(id)load(id);}).observe(document.body,{childList:true,subtree:true});
})();