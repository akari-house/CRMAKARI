(()=>{'use strict';
const $=(s,r=document)=>r.querySelector(s);const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
const page=()=>$('#view-root .page-head h1')?.textContent?.trim()==='Fundraising';
async function api(body){const r=await fetch('/api/fundraising',{method:body?'POST':'GET',credentials:'same-origin',cache:'no-store',headers:{'content-type':'application/json'},body:body?JSON.stringify(body):undefined});const p=await r.json().catch(()=>({}));if(!r.ok)throw new Error(p.error||`Request failed (${r.status})`);return p;}
function toast(m,t='success'){const root=$('#toast-root');if(!root)return;const n=document.createElement('div');n.className=`toast ${t}`;n.textContent=m;root.append(n);setTimeout(()=>n.remove(),3500);}
function modal(){let n=$('#fundraising-dataroom-root');if(!n){n=document.createElement('div');n.id='fundraising-dataroom-root';document.body.appendChild(n);}return n;}
function option(items,value,label='name'){return items.map(x=>`<option value="${esc(x.id)}" ${x.id===value?'selected':''}>${esc(x[label]||x.investorName||x.title)}</option>`).join('');}
async function openRoom(roomId){const data=await api();const room=(data.items||[]).find(x=>x.id===roomId);if(!room)throw new Error('Capital Room was not found');const investors=room.investorPipeline||[];const s=room.diligenceSummary||{};modal().innerHTML=`<div class="dr-backdrop"><section class="dr-modal" role="dialog" aria-label="Fundraising data room"><header><div><div class="eyebrow">CAPITAL BY AKARI</div><h2>${esc(room.projectName)} — Data Room & Diligence</h2><p>Controlled investor access, NDA status, diligence and questions.</p></div><button class="dr-close" data-dr-close>×</button></header><div class="dr-kpis"><article><span>Documents</span><strong>${s.documents||0}</strong></article><article><span>Active access</span><strong>${s.activeAccess||0}</strong></article><article><span>Open diligence</span><strong>${s.openRequests||0}</strong></article><article><span>Open questions</span><strong>${s.openQuestions||0}</strong></article></div><div class="dr-grid"><section><h3>Data room documents</h3><div class="dr-list">${(room.dataRoomDocuments||[]).map(d=>`<article><strong>${esc(d.title)}</strong><span>${esc(d.category)} · ${esc(d.confidentiality)} · v${esc(d.version)}</span><a href="${esc(d.url)}" target="_blank" rel="noopener">Open secure link</a></article>`).join('')||'<p class="muted">No documents added.</p>'}</div><form data-dr-form="document"><input type="hidden" name="roomId" value="${esc(room.id)}"><label>Title<input name="title" required></label><label>Category<select name="category">${['PITCH','FINANCIAL','LEGAL','PRODUCT','TRACTION','TEAM','CAP_TABLE','SECURITY','OTHER'].map(v=>`<option>${v}</option>`).join('')}</select></label><label>Secure link<input name="url" type="url" required></label><label>Version<input name="version" value="1.0"></label><button class="btn primary">Add document</button></form></section><section><h3>Investor access & NDA</h3><div class="dr-list">${(room.investorAccess||[]).map(a=>{const inv=investors.find(i=>i.id===a.investorPipelineId);return`<article><strong>${esc(inv?.investorName||'Investor')}</strong><span>NDA ${esc(a.ndaStatus)} · Access ${esc(a.accessStatus)}</span></article>`;}).join('')||'<p class="muted">No investor access records.</p>'}</div><form data-dr-form="access"><input type="hidden" name="roomId" value="${esc(room.id)}"><label>Investor<select name="investorPipelineId" required><option value="">Select investor</option>${option(investors,'','investorName')}</select></label><label>NDA status<select name="ndaStatus">${['NOT_REQUIRED','REQUESTED','PENDING','SIGNED','REJECTED'].map(v=>`<option>${v}</option>`).join('')}</select></label><label>Access status<select name="accessStatus">${['PENDING','GRANTED','SUSPENDED','REVOKED'].map(v=>`<option>${v}</option>`).join('')}</select></label><label>Expires<input name="expiresAt" type="date"></label><button class="btn primary">Save access</button></form></section><section><h3>Due diligence requests</h3><div class="dr-list">${(room.diligenceRequests||[]).map(d=>`<article><strong>${esc(d.title)}</strong><span>${esc(d.status)}${d.dueDate?` · due ${esc(d.dueDate)}`:''}</span>${d.evidenceUrl?`<a href="${esc(d.evidenceUrl)}" target="_blank" rel="noopener">Evidence</a>`:''}</article>`).join('')||'<p class="muted">No diligence requests.</p>'}</div><form data-dr-form="diligence"><input type="hidden" name="roomId" value="${esc(room.id)}"><label>Investor<select name="investorPipelineId"><option value="">General request</option>${option(investors,'','investorName')}</select></label><label>Request title<input name="title" required></label><label>Status<select name="status">${['OPEN','IN_PROGRESS','WAITING_INVESTOR','WAITING_FOUNDER','RESOLVED','CLOSED'].map(v=>`<option>${v}</option>`).join('')}</select></label><label>Due date<input name="dueDate" type="date"></label><label class="full">Response<textarea name="response"></textarea></label><label class="full">Evidence link<input name="evidenceUrl" type="url"></label><button class="btn primary">Save request</button></form></section><section><h3>Investor questions</h3><div class="dr-list">${(room.investorQuestions||[]).map(q=>`<article><strong>${esc(q.question)}</strong><span>${esc(q.status)}</span>${q.answer?`<p>${esc(q.answer)}</p>`:''}</article>`).join('')||'<p class="muted">No investor questions.</p>'}</div><form data-dr-form="question"><input type="hidden" name="roomId" value="${esc(room.id)}"><label>Investor<select name="investorPipelineId" required><option value="">Select investor</option>${option(investors,'','investorName')}</select></label><label>Status<select name="status"><option>OPEN</option><option>ANSWERED</option></select></label><label class="full">Question<textarea name="question" required></textarea></label><label class="full">Answer<textarea name="answer"></textarea></label><button class="btn primary">Save question</button></form></section></div></section></div>`;
modal().querySelectorAll('[data-dr-form]').forEach(form=>form.addEventListener('submit',async e=>{e.preventDefault();const values=Object.fromEntries(new FormData(form).entries());const kind=form.dataset.drForm;const action={document:'upsert-document',access:'upsert-access',diligence:'upsert-diligence',question:'upsert-question'}[kind];try{await api({action,roomId:values.roomId,item:values});toast('Fundraising workspace updated');await openRoom(room.id);}catch(err){toast(err.message,'error');}}));}
async function mount(){if(!page()||$('#fundraising-dataroom-actions'))return;try{const data=await api();if(!page())return;const section=document.createElement('section');section.id='fundraising-dataroom-actions';section.className='dr-launch';section.innerHTML=`<div><strong>Data Room & Due Diligence</strong><span>Secure investor access, NDA controls, diligence requests and questions.</span></div><div>${(data.items||[]).map(r=>`<button class="btn" data-dr-room="${esc(r.id)}">${esc(r.projectName)}</button>`).join('')||'<span class="muted">Create a Capital Room first.</span>'}</div>`;$('#capital-room-command-centre')?.append(section);}catch(e){console.warn(e);}}
document.addEventListener('click',e=>{const room=e.target.closest('[data-dr-room]');if(room){e.preventDefault();openRoom(room.dataset.drRoom).catch(err=>toast(err.message,'error'));}if(e.target.closest('[data-dr-close]')||e.target.classList.contains('dr-backdrop'))modal().innerHTML='';},true);new MutationObserver(mount).observe(document.documentElement,{childList:true,subtree:true});document.addEventListener('DOMContentLoaded',mount);})();
