const form=document.querySelector('#waitlist-form');const status=document.querySelector('#waitlist-status');form?.addEventListener('submit',async(event)=>{event.preventDefault();status.textContent='Joining the waitlist…';const data=new FormData(form);const payload=Object.fromEntries(data.entries());payload.priorityFeatures=data.getAll('priorityFeatures').slice(0,3);try{const response=await fetch('/api/waitlist',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload)});const result=await response.json();if(!response.ok)throw new Error(result.error||'Unable to join');form.reset();status.textContent=`You're on the waitlist${result.position?` — position #${result.position}`:''}. We will invite teams in batches.`;}catch(error){status.textContent=error.message||'Unable to join the waitlist.';}});